# Javascript-BAsicpractice
# Secure Password Generator 🔐

A clean, responsive, and easy-to-use web application that generates highly secure, random passwords based on user-selected criteria. 

## ✨ Features

* **Customizable Length:** Choose any password length between 4 and 50 characters.
* **Character Toggles:** Granular control over the password composition:
  * Include/Exclude Uppercase Letters (A-Z)
  * Include/Exclude Lowercase Letters (a-z)
  * Include/Exclude Numbers (0-9)
  * Include/Exclude Special Symbols (!@#$%^&*, etc.)
* **One-Click Copy:** Easily copy the generated password to your clipboard with visual feedback.
* **Modern UI:** A sleek, dark-themed user interface that is fully responsive.

## 🛠️ Technologies Used

This project is built using standard web technologies with zero external dependencies:

* **HTML5:** Semantic structure of the application.
* **CSS3:** Styling, flexbox layout, and hover transitions.
* **Vanilla JavaScript (ES6+):** Logic for randomization, DOM manipulation, and the Clipboard API.

## 🚀 How to Use

Because this is a vanilla web project, there is no build process or package installation required.

1. **Download or Clone** this repository to your local machine.
2. Ensure all three files (`index.html`, `style.css`, and `script.js`) are in the same folder.
3. **Open `index.html`** in your favorite web browser (Chrome, Firefox, Safari, Edge, etc.).
4. Adjust your desired password settings and click **Generate Password**.
5. Click the **Copy** button to copy the result to your clipboard.

## 📂 File Structure

\`\`\`text
├── index.html   # The main HTML document
├── style.css    # The stylesheet for the dark UI
├── script.js    # The logic and password generation algorithm
└── README.md    # This file
\`\`\`

## 📝 License

This project is open-source and free to use or modify for personal or commercial purposes.
